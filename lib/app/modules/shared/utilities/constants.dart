import 'package:flutter/material.dart';

final Color corFundo = Color.fromARGB(255, 255, 255, 255);

final Color corSecundaria = Color.fromARGB(255, 95, 0, 218);

final Color dinheiroAtingiu = Color.fromARGB(255, 3, 200, 219);

final Color dinheiroMeta = Color.fromARGB(255, 72, 66, 163);

final Color corInicio = Color.fromARGB(255, 202, 103, 2);

final Color corLetraCinzaEscuro = Color.fromARGB(255, 80, 76, 75);

final Color corLetraCinzaClaro = Color.fromARGB(255, 111, 111, 111);

final Color corBrancoEscuro = Color.fromARGB(255, 245, 245, 245);

final Color corCinzaClaro = Color.fromARGB(255, 200, 200, 200);

final Color corVerde = Color.fromARGB(255, 0, 250, 10);

  final styleTextRendimentoInvestimento1 = TextStyle(
    color: corLetraCinzaClaro,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 12.0,
  );
  
    final styleTextRendimentoInvestimento2 = TextStyle(
    color: corLetraCinzaEscuro,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 24.0,
  );

   final styleTextApresentacao = TextStyle(
    color: Colors.white,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 28.0,
  );

     final styleTextlongo = TextStyle(
    color: Colors.white,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 22.0,
  );

  final styleBoxTotalInvestimento = BoxDecoration(
    color: corBrancoEscuro,
    borderRadius: BorderRadius.circular(10.0),
    boxShadow: [
      BoxShadow(
        color: Colors.black,
        blurRadius: 5.0,
        offset: Offset(3, 1),
      ),
    ],
  );

  final styleBoxEditSonho = BoxDecoration(
    color: corFundo,
    borderRadius: BorderRadius.circular(10.0),
    boxShadow: [
      BoxShadow(
        color: Colors.black,
        blurRadius: 3.0,
        offset: Offset(2, 1),
      ),
    ],
  );

  final styleBoxInformacaoSonho = BoxDecoration(
    color: corFundo,
    borderRadius: BorderRadius.circular(10.0),
    boxShadow: [
      BoxShadow(
        color: Colors.black,
        blurRadius: 5.0,
        offset: Offset(3, 1),
      ),
    ],
  );

  final styleBoxPesquisaInvestimento = BoxDecoration(
    color: corFundo,
    borderRadius: BorderRadius.circular(10.0),
    boxShadow: [
      BoxShadow(
        color: Colors.black,
        blurRadius: 2.0,
        offset: Offset(3, 1),
      ),
    ],
  );

  final styleletra = TextStyle(
    color: corLetraCinzaEscuro,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 15.0,
  );

  final caixaEscritaBranca = BoxDecoration(
    color: corFundo,
    borderRadius: BorderRadius.circular(10.0),
    boxShadow: [
      BoxShadow(
        color: Colors.black12,
        blurRadius: 6.0,
        offset: Offset(0, 2),
      ),
    ],
  );

    final styleTextLetraBTNInvestimento = TextStyle(
    color: Colors.white,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 20.0,
  );
  
  final styleTextLetraInvestimento1 = TextStyle(
    color: corLetraCinzaEscuro,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 12.5,
  );
  
    final styleTextLetraInvestimento2 = TextStyle(
    color: corLetraCinzaClaro,
    fontWeight: FontWeight.bold,
    fontFamily: 'OpenSans',
    fontSize: 13.0,
  );
  